{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "updater",
  "description": "Main-window-only updater checks. Signature verification is mandatory when endpoints are enabled.",
  "windows": ["main"],
  "permissions": [
    "updater:default",
    "process:allow-restart"
  ]
}
