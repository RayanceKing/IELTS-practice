# Progress

- 2026-07-15: Scoped task plan created; implementation inspection started.
- 2026-07-15: Located exact catalog, startup, evaluation adapter, and history adapter boundaries.
- 2026-07-15: A parallel source read failed because the assumed `crates/ielts-domain/src/domain/writing.rs` path does not exist. The DTO read completed; future domain inspection will locate the actual module first instead of repeating the path assumption.
- 2026-07-15: Confirmed seed packaging gap, evaluation default-mode regression, and exact history VM incompatibility.
- 2026-07-15: Verified component-level reading history field contract and existing draft mode mapping.
- 2026-07-15: A guessed `useReadingHistory.js` path was absent; component/template inspection still succeeded. Future lookup will use `rg --files` first rather than retrying that guessed filename.
- 2026-07-15: A second combined read repeated the missing `.js` variant after the `.ts` module had been located, causing the batch to fail. Do not request the nonexistent variant again; `useReadingHistory.ts` delegates directly to `readingHistoryApi` and `readingHistoryCore`.
- 2026-07-15: Collected the immutable reading pack seed pattern, exact Rust history DTO, and writing draft test call sites. Ready to patch bounded implementation.
- 2026-07-15: A combined frontend mode patch was rejected because its assumed `client.js` import block did not match the current file. No part of that patch applied. Next step is to read exact import anchors and patch each frontend file independently; do not retry the broad multi-file patch.
- 2026-07-15: Added the packaged catalog seed implementation/config/startup path plus fixture and shipped-catalog tests. Added explicit frontend `free` → `freeform` mapping and made evaluation/draft APIs reject omitted modes; Compose now passes the mode.
- 2026-07-15: Focused Rust tests (20), new Node tests (2), Vue typecheck, and diff check passed. A combined format/host-check batch stopped at `rustfmt --check`: it found mechanical formatting in this batch plus pre-existing unowned lines reached through `lib.rs` module traversal. Do not run recursive `rustfmt` on `lib.rs`; manually apply the shown formatting to owned hunks, then use `skip_children=true` for the host file and run cargo check separately.
- 2026-07-15: Manual formatting of owned hunks, targeted rustfmt checks, and `cargo check -p ielts-practice-tauri --locked` now pass. A direct caller audit found `EvaluatingPage` retry still omits mode and must be corrected before handoff.
- 2026-07-15: A parallel source read failed only because `crates/ielts-domain/src/dto/writing.rs` was a guessed path. The actual evaluation DTO is in `dto/evaluation.rs`; do not repeat the guessed path.
- 2026-07-15: Final focused test batch exposed a compile error introduced while making `WritingDraft.mode` optional: the patch also wrapped the `AttemptRecord.mode` initializer and left the `WritingDraft` return unwrapped. The error is exact (`draft.rs:56` / `:110`); repair those two assignments, then rerun the same focused suite once.
- 2026-07-15: After repairing the type mismatch, the next batch stopped only at `rustfmt --check` for one new Phase 5 assertion chain. The compiler/test output was not collected because the parallel batch short-circuited; format that one owned assertion and run the focused commands separately to avoid masking results.
- 2026-07-15: Corrected the assertion formatting, then all focused checks passed: Phase 5 writing 14/14, topic seed 6/6, Tauri cargo check, two Node adapter/mode tests, Vue typecheck, diff check, and targeted rustfmt checks. The unowned `draft.rs` historical formatting divergence remains intentionally untouched.
- 2026-07-15: Added catalog resource assertions to the official Tauri static contract. Mandatory full gates passed in order: static 13/13, then fresh packaged Tauri E2E with release build (`b5376a…daf2`), 12/12 checks passed and Reading P95 199ms / 3000ms. Reports/screenshots are ignored test artifacts and remain out of the staged product scope.
